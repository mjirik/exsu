language: python
python: 2.7
os: linux
# Ubuntu 14.04 Trusty support
sudo: required
# dist: trusty
# install new cmake
#addons:
#  apt:
#    packages:
#      - cmake
#    sources:
#      - kalakris-cmake
env:
    # - CONDA_PYTHON_VERSION=2.7
    - CONDA_PYTHON_VERSION=3.6
    - CONDA_PYTHON_VERSION=3.7
    - CONDA_PYTHON_VERSION=3.8
services:
  - xvfb

matrix:
  allow_failures:
    - env: CONDA_PYTHON_VERSION=2.7
    - env: CONDA_PYTHON_VERSION=3.8
  fast_finish: true
# virtualenv:
#   system_site_packages: true
before_script:
    # GUI
    - "export DISPLAY=:99.0"
    # - "sh -e /etc/init.d/xvfb start"
    # - sleep 3 # give xvfb sume time to start

before_install:
    - sudo apt-get update
    - sudo apt-get install -qq cmake libinsighttoolkit3-dev libpng12-dev libgdcm2-dev
    
    - wget http://home.zcu.cz/~mjirik/lisa/install/install_conda.sh && source install_conda.sh
    # We do this conditionally because it saves us some downloading if the
    # version is the same.
    # - if [[ "$CONDA_PYTHON_VERSION" == "2.7" ]]; then
    #     echo "python 2"
    #   else
    #     echo "python 3"
    #   fi
    - hash -r
    - conda config --set always_yes yes --set changeps1 no
    - conda config --add channels mjirik
    - conda config --add channels conda-forge
    - conda update -q conda
    # Useful for debugging any issues with conda
    - conda info -a

# command to install dependencies
install:

    # - sudo apt-get install -qq $(< apt_requirements.txt)
#    - conda create --yes -n travis python=$CONDA_PYTHON_VERSION
    - conda create -n travis --yes --file requirements_conda.txt python=$CONDA_PYTHON_VERSION pytest-cov coveralls pytest-mypy
    - source activate travis
#    - Install dependencies
#    - conda install --yes coveralls pytest pytest-cov
#    - pip install -r requirements_pip.txt
#    - "echo $LD_LIBRARY_PATH"
#    - "pip install -r requirements.txt"
#    - 'mkdir build'
#    - "cd build"
#    - "cmake .."
#    - "cmake --build ."
#    - "sudo make install"
#    - pip install .
#    - "cd .."
#    - 'echo "include /usr/local/lib" | sudo tee -a /etc/ld.so.conf'
#    - 'sudo ldconfig -v'
#    - conda list -e
#    - python -m io3d.datasets -l 3Dircadb1.1 jatra_5mm exp_small sliver_training_001 io3d_sample_data head volumetrie
# command to run tests
# script: nosetests -v --with-coverage --cover-package=exsu

script:
  - python -m pytest --cov=exsu/ -v --mypy --mypi-ignore-missing-imports
after_success:
    - coveralls
